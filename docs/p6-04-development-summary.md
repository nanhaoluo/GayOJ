# P6-04 比赛系统修补总结

## 完成内容

- 打印台纯净页从单次源码预览补齐为打印申请工作台，支持选手申请、待处理列表、已处理列表、源码详情读取、标记已打印和取消。
- 裁判组纯净页接入打印申请统计和打印台入口，保持只展示比赛执法目标内容、返回按钮和必要操作。
- 保留既有比赛打印权限边界：打印只能读取已提交源码或本次请求源码文本，不编译、不运行、不本地判题。
- 保留比赛打印资源归属校验：学生只能查看自己的打印单，裁判和管理员只能处理当前比赛内打印单。
- 外榜、实时外榜、滚榜、裁判组、打印台、气球台等单功能页面继续保持纯净布局，不复用全站导航壳。
- 普通题面和比赛题面不新增 `judge_config` 暴露面。

## 验证

- `npm run check:openapi`：通过。
- `py -3.12 -m pytest apps/api/tests/test_p6_contest_boundaries.py`：54 passed。
- `npm run build:web`：通过。
- `npm run check:api`：272 passed。

## 边界确认

- 比赛题面、提交、Clarification、打印、封榜、重测仍走认证、权限和资源归属校验。
- 代码打印只处理提交源码或请求源码文本，未引入用户代码执行路径。
- 训练赛和正式赛共用同一比赛打印、裁判组和纯净页链路。
- 当前分支已排除疑似 P6-11 题号映射后端漂移，该漂移已保全到 stash，未纳入 P6-04 交付。

## 剩余风险

- 打印状态仍是轻量工作流，未接入真实打印机队列、打印机回执或物理打印失败重试。
- `main` 当前在 `E:\project\ctOJ-p6-06-repair` worktree 存在未解决冲突，本次 P6-04 分支无法安全合并入 `main`；需先清理该 worktree 的冲突后再执行合并。
